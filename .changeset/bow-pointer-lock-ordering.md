---
---

Test-only change: no package version moves and nothing ships to players. Recorded as an empty changeset because the release gate requires one for any diff outside `docs/` and `.github/`, including a change that alters no shipped behaviour.
