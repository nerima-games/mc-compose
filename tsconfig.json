{
  // Editor / language-server default project: everything in the repository.
  // CI gates use tsconfig.build.json (shipped source), tsconfig.test.json
  // (tests + tooling) and tsconfig.preview.json (apps/e2e); this file exists
  // so that opening any file in an editor resolves against the same options.
  "extends": "./tsconfig.test.json"
}
